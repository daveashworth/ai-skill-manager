# Napkin

## Corrections
| Date | Source | What Went Wrong | What To Do Instead |
|------|--------|----------------|-------------------|
| 2026-04-13 | self | Assumed this UI change might live in a JS app and probed for `package.json` before checking the actual project shape. | Confirm the repo stack from the file tree first; this project is a Rust `ratatui` TUI. |

## User Preferences
- Keep UI tweaks focused on the requested change instead of broad refactors.
- Prefer subtler UI accents over loud highlight colors in this TUI.
- Group labels in the skill list still need enough contrast to be identifiable at a glance.

## Patterns That Work
- UI list rendering for the main screen lives in [`src/ui.rs`](file:///Users/daveashworth/data/projects/skill-manager/src/ui.rs); small presentation-only changes can stay there without touching app state.
- Release artifacts for this repo come from `cargo build --release`, producing `target/release/skill-manager` as documented in `README.md`.

## Patterns That Don't Work
- Inferring the stack from the feature request wording alone is unreliable in this repo.

## Domain Notes
- Skill group membership is stored in `Config.groups` and looked up by canonical skill key.
